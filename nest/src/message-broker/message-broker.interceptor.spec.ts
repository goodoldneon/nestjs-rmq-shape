import { MessageBrokerInterceptor } from './message-broker.interceptor';

describe('MessageBrokerInterceptor', () => {
  it('should be defined', () => {
    expect(new MessageBrokerInterceptor()).toBeDefined();
  });
});
